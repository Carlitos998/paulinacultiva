{
  "name": "proyecto-unificado",
  "version": "1.0.0",
  "description": "Proyecto unificado con backend de Toby y frontend de Noa",
  "scripts": {
    "install-all": "npm install && (cd api && npm install) && (cd client && npm install)",
    "dev-api": "cd api && npm start",
    "dev-client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd api && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
